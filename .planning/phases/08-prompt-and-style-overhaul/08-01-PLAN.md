---
phase: 08-prompt-and-style-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/prompts.ts]
autonomous: true
requirements: [PRMT-01, PRMT-02, PRMT-03, PRMT-04, PRMT-05]

must_haves:
  truths:
    - "buildMenuPrompt() output contains four labeled sections: [STYLE DIRECTIVE], [LAYOUT], [CONTENT], [CONSTRAINTS]"
    - "Every generated prompt includes 8 negative constraints (no food photos, no clipart, etc.)"
    - "LAYOUT section contains 5-layer typography hierarchy instructions"
    - "LAYOUT section opens with human-readable aspect ratio description"
    - "EN/FR prompts include enhanced culinary terminology and accent preservation instructions"
    - "Logo instructions from Phase 7 are preserved verbatim in CONSTRAINTS section"
    - "Personalizado (empty stylePrompt) omits [STYLE DIRECTIVE] section cleanly"
  artifacts:
    - path: "lib/prompts.ts"
      provides: "Modular buildMenuPrompt() with four-section structure"
      exports: ["buildMenuPrompt", "PromptInput"]
  key_links:
    - from: "lib/prompts.ts"
      to: "app/api/generate/route.ts"
      via: "buildMenuPrompt(input) call"
      pattern: "buildMenuPrompt"
---

<objective>
Restructure buildMenuPrompt() in lib/prompts.ts from a flat monolithic prompt into a four-section modular structure: [STYLE DIRECTIVE] -> [LAYOUT] -> [CONTENT] -> [CONSTRAINTS]. Add negative prompting, text hierarchy instructions, aspect ratio text descriptions, and enhanced language instructions for EN/FR.

Purpose: The modular structure gives Gemini clearer instruction hierarchy, improving image quality and consistency. Negative prompts prevent common AI artifacts. Text hierarchy ensures professional typography layering.

Output: Rewritten lib/prompts.ts with the same PromptInput interface and buildMenuPrompt() export signature, producing structured multi-section prompts.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-logo-integration/07-01-SUMMARY.md
@.planning/phases/08-prompt-and-style-overhaul/08-RESEARCH.md

<interfaces>
<!-- Current lib/prompts.ts — the file being rewritten. Executor MUST preserve the export signature. -->

From lib/prompts.ts:
```typescript
export interface PromptInput {
  stylePrompt: string
  dishes: { category: string; name: string }[]
  price: string
  dateRange: string
  aspectRatio: string
  menuType: 'weekly' | 'event'
  eventTitle?: string
  language?: Language
}

export function buildMenuPrompt(input: PromptInput): string
```

From lib/types.ts:
```typescript
export type Language = 'es' | 'en' | 'fr'
```

<!-- Logo instruction lines from Phase 7 (lines 76-81 of current prompts.ts) that MUST be preserved verbatim in the new CONSTRAINTS section: -->

```
- The first inline image provided is the GIA restaurant logo — it MUST appear in the generated image.
- Place the logo at the top center of the menu, centered horizontally, within the top 20% of the image.
- The logo should occupy approximately 15% of the image width.
- Preserve the logo's original warm beige/sandy color (approximately #D4B49A) EXACTLY — do not recolor, tint, adjust brightness, or alter the logo in any way.
- The logo should appear naturally integrated into the header area, above all menu text content.
- Do NOT include any watermarks, other logos, or extra branding — only the GIA logo provided.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite buildMenuPrompt() with modular four-section structure</name>
  <files>lib/prompts.ts</files>
  <action>
Rewrite lib/prompts.ts. Keep the same `PromptInput` interface and `buildMenuPrompt()` export signature. Decompose the function into four helper functions that each build one section:

1. **buildStyleSection(stylePrompt: string | undefined):** Returns `[STYLE DIRECTIVE]\n${stylePrompt}` if stylePrompt is truthy and non-empty after trim. Returns `null` if empty/falsy (Personalizado case).

2. **buildLayoutSection(aspectRatio: string):** Returns the [LAYOUT] section containing:
   - Opening line: `Restaurant menu card in {description}. Print-quality design.` where description comes from an `aspectRatioDescriptions` Record mapping ratio codes to human-readable text (PRMT-04):
     - `'3:4'`: `'portrait orientation, 3:4 aspect ratio (standard menu card)'`
     - `'2:3'`: `'portrait orientation, 2:3 aspect ratio (tall menu card)'`
     - `'4:3'`: `'landscape orientation, 4:3 aspect ratio'`
     - `'9:16'`: `'portrait orientation, 9:16 aspect ratio (vertical social media story)'`
     - `'16:9'`: `'landscape orientation, 16:9 aspect ratio (horizontal banner)'`
     - `'1:1'`: `'square format, 1:1 aspect ratio'`
   - Layout guidance: single-column centered, generous margins (15-20% on each side)
   - Typography hierarchy block (PRMT-03) — exactly these 5 layers:
     ```
     TYPOGRAPHY HIERARCHY:
     - Layer 1 — Restaurant name "Gia": largest element, decorative display serif or bold display font, top of image, centered
     - Layer 2 — Section headers (Primeros, Segundos, Postre): medium size, uppercase with wide letter-spacing, clearly distinct from dish names
     - Layer 3 — Dish names: clearly readable, medium-weight serif or sans-serif, left-aligned or centered within their section
     - Layer 4 — Dish descriptions (if any): smaller, lighter weight, italic or muted color
     - Layer 5 — Price: right-aligned or inline, same size as dish names or smaller, never bolder than dish names
     ```
   - Typography constraints: "maximum two typefaces", "body text minimum medium weight (not thin/hairline)", "letter-spacing on all uppercase text"

3. **buildContentSection(input: PromptInput, labels: Record<string, string>):** Returns the [CONTENT] section containing:
   - Restaurant name, location, menu type label, date range
   - Dish sections with **quoted** dish names (`"${d.name}"` not `${d.name}`) for better Gemini text rendering accuracy
   - Price line
   - Exact dish name list with instruction to render them exactly as written

4. **buildConstraintsSection(input: PromptInput):** Returns the [CONSTRAINTS] section containing:
   - 8 negative prompts (PRMT-02), exactly:
     ```
     - No food photographs or images of food
     - No clipart or cartoon illustrations
     - No extra text beyond what is listed in the CONTENT section above
     - No blurry, garbled, or illegible text
     - No watermarks or extra branding beyond the GIA logo
     - No busy or cluttered background texture that overlaps text
     - No more than two typefaces
     - No thick heavy borders or ornate frames that crowd the text area
     ```
   - Logo instructions — copy the 6 lines from Phase 7 VERBATIM (see interfaces block above). These are tested and must not be paraphrased.
   - Language instructions (PRMT-05) — only included when lang is 'en' or 'fr':
     - For EN: "All text must be in English", "Use proper culinary English: Starters, Main Courses, Desserts", "Preserve correct spelling and capitalization exactly as listed in CONTENT", "Maintain exact same visual layout, colors, typography"
     - For FR: "All text must be in French", "Use proper French culinary terminology: Entrees, Plats, Desserts", "Preserve all French accents exactly: e, e, e, a, a, o, u, u, c, i, i — do NOT simplify or omit", "Maintain exact same visual layout, colors, typography"

The main `buildMenuPrompt()` function joins the four sections with `\n\n`, filtering out nulls (for when style section is omitted).

Keep the existing `categoryLabelsByLang`, `menuTypeLabelByLang`, and `textLangName` constants. The PromptInput interface stays unchanged.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/gia/gia-menu-studio" && npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>
- buildMenuPrompt() compiles and produces a prompt string with [STYLE DIRECTIVE], [LAYOUT], [CONTENT], [CONSTRAINTS] sections separated by blank lines
- Negative prompts (8 items) always present in CONSTRAINTS
- Typography hierarchy (5 layers) always present in LAYOUT
- Aspect ratio described in text in LAYOUT section
- Logo instructions from Phase 7 preserved verbatim in CONSTRAINTS
- EN/FR language instructions included in CONSTRAINTS when language is en/fr
- Empty stylePrompt (Personalizado) results in prompt without [STYLE DIRECTIVE] section
- TypeScript compiles clean, Next.js build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `npx next build` succeeds (no build errors)
- Manual: call buildMenuPrompt() with a sample input and verify the four sections appear with correct headers
- Manual: call with empty stylePrompt and verify [STYLE DIRECTIVE] is omitted
- Manual: call with language='en' and verify enhanced English instructions appear in CONSTRAINTS
</verification>

<success_criteria>
- lib/prompts.ts exports the same PromptInput interface and buildMenuPrompt() function
- Output prompt has four labeled sections in order: STYLE -> LAYOUT -> CONTENT -> CONSTRAINTS
- All 5 PRMT requirements addressed (modular structure, negatives, hierarchy, aspect ratio, language)
- Phase 7 logo instructions preserved verbatim
- TypeScript and Next.js build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-and-style-overhaul/08-01-SUMMARY.md`
</output>
