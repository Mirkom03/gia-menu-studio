---
phase: 08-prompt-and-style-overhaul
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified: [supabase/migrations/003_replace_styles.sql]
autonomous: false
requirements: [STYL-01, STYL-02, STYL-03, STYL-04]

must_haves:
  truths:
    - "Migration file deletes all existing styles and inserts 8 new archetypes + Personalizado"
    - "Each archetype has a multi-paragraph prompt_template (8-12 lines) with specific hex colors, typography, decorative elements, and mood"
    - "Each archetype has a colors JSONB object with background, primary, accent, secondary keys matching the hex values in its prompt_template"
    - "Personalizado is the 9th entry with empty prompt_template and empty colors object"
    - "Style names are in Spanish (matching the Spanish UI)"
    - "StyleGallery gradient previews render correctly using the new colors JSONB"
  artifacts:
    - path: "supabase/migrations/003_replace_styles.sql"
      provides: "Complete style replacement migration with 9 style rows"
      contains: "DELETE FROM styles"
  key_links:
    - from: "supabase/migrations/003_replace_styles.sql"
      to: "styles table"
      via: "DELETE + INSERT SQL"
      pattern: "INSERT INTO styles"
    - from: "styles.colors JSONB"
      to: "components/style-gallery.tsx"
      via: "buildGradient() reads Object.values(colors)"
      pattern: "buildGradient"
    - from: "styles.prompt_template"
      to: "lib/prompts.ts"
      via: "input.stylePrompt passed to buildStyleSection()"
      pattern: "stylePrompt"
---

<objective>
Create a new SQL migration that replaces the 5 current one-liner style presets with 8 research-backed archetypes plus Personalizado. Each archetype gets a multi-paragraph prompt_template and accurate colors JSONB for gradient preview cards.

Purpose: Multi-paragraph style templates give Gemini specific visual direction (colors, typography, decoration, mood) instead of generic one-liners, producing professional-looking menu images. The colors JSONB drives accurate gradient previews in StyleGallery.

Output: supabase/migrations/003_replace_styles.sql ready to apply via Supabase Dashboard SQL Editor.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-prompt-and-style-overhaul/08-RESEARCH.md
@.planning/phases/08-prompt-and-style-overhaul/08-01-SUMMARY.md

<interfaces>
<!-- Style interface from lib/types.ts — the columns the migration must populate -->

From lib/types.ts:
```typescript
export interface Style {
  id: string           // UUID, auto-generated
  name: string         // Display name in StyleGallery
  description: string  // Short description in Spanish
  prompt_template: string  // Multi-paragraph style directive (TEXT column, unlimited)
  preview_url: string | null  // NULL for now
  colors: Record<string, string>  // JSONB with background, primary, accent, secondary
  is_active: boolean   // Must be true for all new styles
  sort_order: number   // 1-9 ordering
  created_at: string   // Auto-generated
}
```

<!-- buildGradient() from components/style-gallery.tsx — reads Object.values(colors) -->

From components/style-gallery.tsx:
```typescript
function buildGradient(colors: Record<string, string>): string {
  const values = Object.values(colors)       // reads all values from JSONB
  if (values.length === 0) { ... fallback }  // handles Personalizado {} correctly
  // builds gradient from hex stops
}
```

<!-- Current migration pattern from 002_seed_styles.sql -->
```sql
INSERT INTO styles (name, description, prompt_template, sort_order) VALUES (...)
```
Note: The new migration MUST also include `colors` and `is_active` columns in the INSERT.

<!-- The 8 archetypes with their prompt_templates and colors are fully specified in 08-RESEARCH.md sections "8 Style Archetypes" and "Personalizado Handling" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write style replacement migration with 8 archetypes + Personalizado</name>
  <files>supabase/migrations/003_replace_styles.sql</files>
  <action>
Create supabase/migrations/003_replace_styles.sql that:

1. Opens with a comment: `-- Replace 5 one-liner style presets with 8 research-backed archetypes + Personalizado`
2. `DELETE FROM styles;` to clear all existing rows
3. Single INSERT statement with 9 rows, each specifying: name, description, prompt_template, colors (JSONB cast), is_active, sort_order

The 9 styles in order (sort_order 1-9). Use Spanish names for display:

| sort_order | English archetype | Spanish name |
|---|---|---|
| 1 | Classic Trattoria | Trattoria Clasica |
| 2 | Elegant Fine Dining | Alta Cocina Elegante |
| 3 | Rustic Mediterranean | Mediterraneo Rustico |
| 4 | Modern Minimalist | Minimalista Moderno |
| 5 | Vintage Osteria | Osteria Vintage |
| 6 | Coastal Mediterranean | Costa Mediterranea |
| 7 | Luxe Bistro | Bistro de Lujo |
| 8 | Contemporary Italian | Italiano Contemporaneo |
| 9 | Personalizado | Personalizado |

For each archetype (1-8):
- `description`: Short Spanish description (~1 sentence) summarizing the visual feel
- `prompt_template`: The multi-paragraph template from 08-RESEARCH.md section "8 Style Archetypes" — copy EXACTLY as written in the research document, preserving all hex values, typography guidance, decorative element descriptions, and mood line
- `colors`: JSONB object with exactly 4 keys (background, primary, accent, secondary) using the hex values from 08-RESEARCH.md — MUST match the hex values in the prompt_template
- `is_active`: true

For Personalizado (9):
- `description`: 'Describe tu estilo y lo creamos para ti.'
- `prompt_template`: '' (empty string)
- `colors`: '{}' (empty JSONB object)
- `is_active`: true

CRITICAL: The hex values in `colors` JSONB MUST exactly match the hex values named in the corresponding `prompt_template`. These are the canonical values from 08-RESEARCH.md:

1. Classic Trattoria: {"background": "#F5F0E1", "primary": "#722F37", "accent": "#C8A96E", "secondary": "#5C6B3C"}
2. Elegant Fine Dining: {"background": "#1A1A1A", "primary": "#F2EDE4", "accent": "#C5A55A", "secondary": "#8A8580"}
3. Rustic Mediterranean: {"background": "#E8D5B5", "primary": "#C4694A", "accent": "#8A9A5B", "secondary": "#2C2C2C"}
4. Modern Minimalist: {"background": "#FFFFFF", "primary": "#111111", "accent": "#B86B4A", "secondary": "#E0E0E0"}
5. Vintage Osteria: {"background": "#EDE5D0", "primary": "#5B1A2A", "accent": "#B8860B", "secondary": "#3E2723"}
6. Coastal Mediterranean: {"background": "#FFFFFF", "primary": "#2E6B8A", "accent": "#C67C4E", "secondary": "#E8DCC8"}
7. Luxe Bistro: {"background": "#1B4332", "primary": "#D4AF37", "accent": "#FFFDD0", "secondary": "#F2D7D9"}
8. Contemporary Italian: {"background": "#F8F4EE", "primary": "#333333", "accent": "#B86B4A", "secondary": "#C9A9A6"}

Use proper SQL string escaping (double single quotes for apostrophes within strings). Cast colors with `::jsonb`.
  </action>
  <verify>
    <automated>cd "C:/Users/Lenovo/gia/gia-menu-studio" && test -f supabase/migrations/003_replace_styles.sql && echo "Migration file exists" && grep -c "INSERT INTO styles" supabase/migrations/003_replace_styles.sql</automated>
  </verify>
  <done>
- Migration file exists at supabase/migrations/003_replace_styles.sql
- Contains DELETE FROM styles followed by INSERT with 9 rows
- Each archetype has multi-paragraph prompt_template (8-12 lines)
- Each archetype has colors JSONB with 4 keys matching prompt_template hex values
- Personalizado has empty prompt_template and empty colors
- All styles have is_active = true
- sort_order 1-9 assigned correctly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Apply migration and verify styles in app</name>
  <files>supabase/migrations/003_replace_styles.sql</files>
  <action>
User applies the migration via Supabase Dashboard SQL Editor, then verifies the styles table and StyleGallery UI:
1. Open Supabase Dashboard SQL Editor
2. Copy and paste the contents of supabase/migrations/003_replace_styles.sql
3. Execute the SQL
4. Verify in Table Editor: styles table should have exactly 9 rows with sort_order 1-9
5. Spot-check 2-3 styles: confirm prompt_template is multi-paragraph (not a one-liner) and colors JSONB has 4 hex values
6. Open the app style selection UI (StyleGallery) — verify all 8 styles show gradient preview cards with distinct color combinations
7. Verify Personalizado still appears as the last option with a fallback/default gradient
  </action>
  <verify>User confirms styles table has 9 rows and StyleGallery renders correctly</verify>
  <done>Migration applied, 9 styles visible in DB and StyleGallery with distinct gradient previews</done>
</task>

</tasks>

<verification>
- Migration file exists and is valid SQL
- After applying: 9 rows in styles table, each with correct name, multi-paragraph prompt_template, and matching colors JSONB
- StyleGallery renders 8 distinct gradient preview cards + Personalizado fallback
- Generating a menu with any of the 8 new styles produces a visually distinct result using the modular prompt from Plan 08-01
</verification>

<success_criteria>
- supabase/migrations/003_replace_styles.sql created with 9 style rows
- Migration applied successfully via Supabase Dashboard
- StyleGallery shows 8 gradient cards with visually distinct color palettes
- Personalizado preserved as 9th option with empty prompt and fallback gradient
</success_criteria>

<output>
After completion, create `.planning/phases/08-prompt-and-style-overhaul/08-02-SUMMARY.md`
</output>
