---
phase: 07-logo-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/logo-gia.png
  - lib/gemini.ts
  - lib/prompts.ts
  - app/api/generate/route.ts
autonomous: false
requirements: [LOGO-01, LOGO-02, LOGO-03]

must_haves:
  truths:
    - "Every generated menu image includes the GIA logo as an inline image part sent to Gemini"
    - "The prompt instructs Gemini to place the logo top-center at ~15% image width"
    - "The prompt instructs Gemini to preserve the logo's original warm beige colors without recoloring"
  artifacts:
    - path: "public/logo-gia.png"
      provides: "GIA logo PNG file accessible at server runtime"
    - path: "lib/gemini.ts"
      provides: "Both generation functions accept logoBase64 and include it as inlineData part"
      exports: ["generateMenuImage", "generateMenuImageWithReference"]
    - path: "lib/prompts.ts"
      provides: "buildMenuPrompt includes logo placement, sizing, and color preservation instructions"
      exports: ["buildMenuPrompt"]
    - path: "app/api/generate/route.ts"
      provides: "API route loads logo from filesystem and passes logoBase64 to generation functions"
  key_links:
    - from: "app/api/generate/route.ts"
      to: "lib/gemini.ts"
      via: "passes logoBase64 to generateMenuImage and generateMenuImageWithReference"
      pattern: "generateMenuImage\\(prompt,\\s*logoBase64"
    - from: "lib/gemini.ts"
      to: "Gemini API"
      via: "inlineData part with logo base64 in contents array"
      pattern: "inlineData.*logo"
    - from: "lib/prompts.ts"
      to: "Generated prompt string"
      via: "Logo placement and color preservation instructions in OUTPUT INSTRUCTIONS"
      pattern: "logo.*top center|preserve.*logo.*color"
---

<objective>
Integrate the GIA logo into every menu image generation by sending it as an inline image part to Gemini, with explicit prompt instructions for placement (top-center, ~15% width) and color preservation (warm beige, no recoloring).

Purpose: Every generated menu image must show the GIA brand logo, positioned consistently and with original colors intact.
Output: Updated generation pipeline (gemini.ts, prompts.ts, route.ts) + logo file in public/.
</objective>

<execution_context>
@C:/Users/Lenovo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Lenovo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-logo-integration/07-RESEARCH.md

@lib/gemini.ts
@lib/prompts.ts
@app/api/generate/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo and update generation functions + prompt builder</name>
  <files>public/logo-gia.png, lib/gemini.ts, lib/prompts.ts</files>
  <action>
1. Copy the logo file into the project:
   - Source: `C:/Users/Lenovo/gia/logo gia-01.png`
   - Destination: `public/logo-gia.png`
   - Use `cp` command (the file is a 52 KB transparent PNG with warm beige serif "GIA" text)

2. Update `lib/gemini.ts` — modify BOTH functions to accept `logoBase64: string` parameter and include it as an `inlineData` part:

   **generateMenuImage()** — Change signature to `(prompt: string, logoBase64: string, aspectRatio: string = '3:4')`. Convert `contents: prompt` (bare string) to a structured contents array:
   ```
   contents: [{
     role: 'user',
     parts: [
       { inlineData: { mimeType: 'image/png', data: logoBase64 } },
       { text: prompt },
     ],
   }]
   ```

   **generateMenuImageWithReference()** — Change signature to `(prompt: string, referenceBase64: string, logoBase64: string, aspectRatio: string = '3:4')`. Add the logo as a second `inlineData` part between the reference image and the text:
   ```
   parts: [
     { inlineData: { mimeType: 'image/png', data: referenceBase64 } },
     { inlineData: { mimeType: 'image/png', data: logoBase64 } },
     { text: `Generate a restaurant menu image following the EXACT same visual style, layout, colors, typography, and decorative elements as the first reference image above. Do NOT copy the content — only replicate the style. The second image is the GIA restaurant logo — it MUST appear in the generated image as specified in the prompt instructions. Use this new content:\n\n${prompt}` },
   ]
   ```

   Keep everything else (model, config, response parsing, error message) exactly the same.

3. Update `lib/prompts.ts` — in `buildMenuPrompt()`, replace the last line of the OUTPUT INSTRUCTIONS block:
   - REMOVE: `- Do NOT include any watermarks or logos other than the restaurant name${languageInstructions}`
   - REPLACE WITH the following lines (before the languageInstructions interpolation):
   ```
   - The first inline image provided is the GIA restaurant logo — it MUST appear in the generated image.
   - Place the logo at the top center of the menu, centered horizontally, within the top 20% of the image.
   - The logo should occupy approximately 15% of the image width.
   - Preserve the logo's original warm beige/sandy color (approximately #D4B49A) EXACTLY — do not recolor, tint, adjust brightness, or alter the logo in any way.
   - The logo should appear naturally integrated into the header area, above all menu text content.
   - Do NOT include any watermarks, other logos, or extra branding — only the GIA logo provided.${languageInstructions}
   ```
   Note: The "Include the restaurant name 'Gia' prominently" line should remain — it reinforces the logo instruction.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
  - public/logo-gia.png exists (copied from source)
  - generateMenuImage accepts logoBase64 and sends it as inlineData part in structured contents array
  - generateMenuImageWithReference accepts logoBase64 as third parameter and sends it as second inlineData part
  - buildMenuPrompt output includes logo placement instructions (top-center, 15% width) and color preservation (#D4B49A)
  - The old "Do NOT include any watermarks or logos" line is replaced with the new logo-aware version
  - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire logo loading in API route and pass to generation functions</name>
  <files>app/api/generate/route.ts</files>
  <action>
1. Add imports at the top of `app/api/generate/route.ts`:
   ```typescript
   import fs from 'fs'
   import path from 'path'
   ```

2. Inside the POST handler, BEFORE the "Generate image" section (before the `const imageBuffer = referenceImage` ternary on ~line 222), add logo loading:
   ```typescript
   // Load GIA logo for inline image injection
   const logoBuffer = fs.readFileSync(path.join(process.cwd(), 'public', 'logo-gia.png'))
   const logoBase64 = logoBuffer.toString('base64')
   ```
   Use per-request `fs.readFileSync` (not module-level singleton) — the file is 52 KB, safe for serverless.

3. Update the generation function calls to pass `logoBase64`:
   - WITH reference: `generateMenuImageWithReference(prompt, referenceImage, logoBase64, ratioString)` — note logoBase64 is now the third argument (before ratioString)
   - WITHOUT reference: `generateMenuImage(prompt, logoBase64, ratioString)` — note logoBase64 is now the second argument (before ratioString)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx next build</automated>
  </verify>
  <done>
  - route.ts imports fs and path
  - route.ts reads public/logo-gia.png and converts to base64
  - Both generateMenuImage and generateMenuImageWithReference calls pass logoBase64
  - TypeScript compiles and Next.js builds successfully
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Logo integration into the menu image generation pipeline. The GIA logo is now sent as an inline image part to Gemini on every generation call, with prompt instructions for top-center placement (~15% width) and color preservation (warm beige #D4B49A).</what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Navigate to the menu generation page
    3. Select any style preset and generate a menu image
    4. Verify in the generated image:
       a. The GIA logo appears (not omitted)
       b. The logo is positioned at the top-center area of the menu
       c. The logo retains its warm beige/sandy color (not white, gold, or dark)
       d. The logo is not oversized or overlapping menu content
    5. Generate a second image with a DIFFERENT style to confirm logo appears consistently
    6. If a reference image is available, test generation with a reference image too
  </how-to-verify>
  <resume-signal>Type "approved" if logo appears correctly, or describe any issues (wrong color, missing, wrong position, etc.)</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compilation passes
2. `npx next build` — Next.js build succeeds
3. Visual: Generated menu images show the GIA logo at top-center with original beige colors
</verification>

<success_criteria>
- public/logo-gia.png exists in the project
- Both generation functions in lib/gemini.ts accept and send logoBase64 as inlineData
- buildMenuPrompt() output includes 6 logo-specific instruction lines
- app/api/generate/route.ts loads the logo and passes it to both generation paths
- Generated images visually confirm logo presence, placement, and color fidelity
</success_criteria>

<output>
After completion, create `.planning/phases/07-logo-integration/07-01-SUMMARY.md`
</output>
